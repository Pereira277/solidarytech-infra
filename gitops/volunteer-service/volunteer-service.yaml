apiVersion: v1
kind: Service
metadata:
  name: volunteer-service
  namespace: solidarytech
spec:
  selector:
    app: volunteer-service
  ports:
    - port: 80
      targetPort: 8083
  type: ClusterIP
