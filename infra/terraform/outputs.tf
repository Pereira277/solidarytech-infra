# Outputs populated as infrastructure resources are provisioned:
#   vpc_id, eks_cluster_endpoint, eks_cluster_name
#   rds endpoints (ngo, donation)
#   dynamodb_table_name (volunteer)
#   sqs_queue_url (donation)
#   velero_s3_bucket (DR)
