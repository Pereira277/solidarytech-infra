# Copy to terraform.tfvars and adjust values as needed.
# terraform.tfvars is git-ignored and must not be committed.

aws_region    = "us-east-1"
aws_region_dr = "us-east-2"
project       = "solidarytech"
environment   = "production"
